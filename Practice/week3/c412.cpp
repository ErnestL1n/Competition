# include <bits/stdc++.h>
using namespace std;
#define mod 1e9+7;

void dfs(int& res,string& s,string& path,int start){
    if(path=="OwO"){
        ++res;
        return;
    }
    for(int i=st;i<s.size();++i){
        if()
    }
}

void foo(){
    string s;
    cin >> s;
    int res=0;
    dfs(res,s,"",0);
    cout << res;
}

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    int t;
    cin>>t;
    while(t--)
        foo();
    return 0;
}